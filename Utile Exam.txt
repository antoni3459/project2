#pragma comment(lib, "Comctl32.lib")
#pragma comment(linker,"/manifestdependency:\"type='win32' name='Microsoft.Windows.Common-Controls' version='6.0.0.0' processorArchitecture='*' publicKeyToken='6595b64144ccf1df' language='*'\"")
#include <CommCtrl.h>



WM_LBUTTONDOWN permet que qand on click dessus donc si on met un boutton spécifique a ça alors les méthod qui sont dans la case doive sortir 
c'est donc par la qu'il faudrait relier avec le menu grâce a une méthod créé dans la windows qui va relier grâce a de nouvelle variable ou a 
a la manière du main faire défiller le menu 

